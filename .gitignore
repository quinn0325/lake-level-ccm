__pycache__/
*.pyc
.DS_Store
.venv/
venv/

# 大体积中间产物：TIFF 版图（单张 24-82 MB）与运行日志不入库
*.tiff
run.log
results/figures/

# Rendered appendix tables are regenerated by code/07_tables/; the report already
# prints them and appendices/*.csv is what verify.py checks, so the Markdown and
# Word renderings are not committed.
appendices/*.md
appendices/en/
appendices/全部附录.xlsx

# Intermediate tables between results/ and the figure scripts. Regenerated in
# under a second by code/07_tables/build_ch4_tables.py and never reported.
ch4_tables/
